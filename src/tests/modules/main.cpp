import std.format;
import std.iostream;
import std.ostream;
import std.string;
#include <stdlib.h>

int main(int argc, char* argv[])
{
  std::cout << "modules: " << std::format("{}", "success") << std::endl;
  return EXIT_SUCCESS;
}
